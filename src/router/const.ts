// you can add any IBA cocktail, and it will dynamically add a new page
const COCKTAILS_PAGES = ["margarita", "mojito", "a1", "kir" /*"americano"*/];

export const PAGES_ARRAY = COCKTAILS_PAGES.map((page) => `/${page}`);
