// you can add any IBA cocktail, and it will dynamically add a new page
const COCKTAILS_PAGES = ["margarita", "mojito", "a1", "kir"];

export const COCKTAILS_PAGES_ARRAY = COCKTAILS_PAGES.map((page) => `/${page}`);
